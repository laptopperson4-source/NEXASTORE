-- Optional notes: create bucket in Dashboard → Storage → New bucket
-- Name: nexastore-avatars
-- Public: YES (so avatar URLs work)
-- Then policies:

-- Allow authenticated users to upload to their folder
-- (configure in Storage policies UI if SQL policies differ by project version)
